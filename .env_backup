# Updated: 2026-05-17 21:05 Asia/Shanghai
# Main changes:
# 1. Added scheduler interval configuration
# 2. Added carbody_history SQL Server source and ODS incremental refresh config
# 3. Previously: Added defect_db summary refresh PostgreSQL target + SQL Server/PostgreSQL source templates
# 4. Kept original WebSocket and rollerbed_tracking_db configs

#---------------------------------------------------------rollerbed_tracking_db------------------------------
# Database connection settings (for Python programs)
DB_HOST=172.22.44.99
DB_PORT=5432
DB_NAME=rollerbed_tracking_db
DB_USER=root
DB_PASSWORD=root

# WebSocket client settings (for data_saver_service_v3.py)
WS_SERVER_HOST=host.docker.internal
WS_SERVER_PORT=8082
WS_SERVER_PATH=/ws/emosweb
WS_USE_PORT_IN_URL=true

# Other settings
LOG_LEVEL=INFO
DEVICE_CONFIG_PATH=deviceConfig.json

#---------------------------------------------------------defect_db------------------------------

# defect_db target database config
DEFECT_TARGET_DB_HOST=localhost
DEFECT_TARGET_DB_PORT=5432
DEFECT_TARGET_DB_NAME=defect_db
DEFECT_TARGET_DB_USER=root
DEFECT_TARGET_DB_PASSWORD=root

# defect_db source database config 172.22.37.52、DENTCHECK sa 密码空  sqlserver 1433 dbo
DEFECT_SOURCE_DB_HOST=172.22.37.52
DEFECT_SOURCE_DB_NAME=DENTCHECK
DEFECT_SOURCE_DB_USER=sa
DEFECT_SOURCE_DB_PASSWORD=
DEFECT_SOURCE_DB_TYPE=sqlserver
DEFECT_SOURCE_DB_PORT=1433
DEFECT_SOURCE_DB_SCHEMA=dbo

# Incremental refresh config
DEFECT_SUMMARY_BATCH_SIZE=2000
DEFECT_SUMMARY_REPLAY_HISTORY_WINDOW=20
DEFECT_SUMMARY_LOCK_KEY=20260413
DEFECT_SUMMARY_BOOTSTRAP_MODE=from_zero
DEFECT_SUMMARY_LOG_LEVEL=INFO
DEFECT_SUMMARY_TIMEZONE=Asia/Shanghai
DEFECT_SUMMARY_RETENTION_MODE=max_rows
DEFECT_SUMMARY_RETENTION_MAX_ROWS=50000
DEFECT_SUMMARY_RETENTION_MAX_MONTHS=3
DEFECT_SUMMARY_RETENTION_DELETE_BATCH_SIZE=5000

#---------------------------------------------------------carbody_history SQL Server Source------------------------------
# carbody history source database (SQL Server)
CARBODY_SOURCE_DB_HOST=localhost
CARBODY_SOURCE_DB_PORT=1433
CARBODY_SOURCE_DB_NAME=DXQcontrol_SVWMEB_BI_DWH
CARBODY_SOURCE_DB_USER=sa
CARBODY_SOURCE_DB_PASSWORD=Sa123456!          # ← fill in actual password
CARBODY_SOURCE_DB_SCHEMA=dwh
CARBODY_SOURCE_TABLE_NAME=MDS_HISTORIC
CARBODY_TARGET_DB_HOST=localhost
CARBODY_TARGET_DB_PORT=5432
CARBODY_TARGET_DB_NAME=analytics_db
CARBODY_TARGET_DB_USER=root
CARBODY_TARGET_DB_PASSWORD=root

# carbody ODS incremental refresh configuration
CARBODY_REFRESH_LOCK_KEY=20260515
CARBODY_REFRESH_BATCH_SIZE=5000
CARBODY_REFRESH_LOG_LEVEL=INFO
CARBODY_REFRESH_TIMEZONE=Asia/Shanghai

#---------------------------------------------------------scheduler------------------------------
# 定时调度器的刷新时间间隔（单位：分钟），默认是 3 分钟
SCHEDULER_INTERVAL_MINUTES=3
