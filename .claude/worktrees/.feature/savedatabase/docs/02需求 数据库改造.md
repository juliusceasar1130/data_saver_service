# 基于 1_最终数据库设计方案_mysql版本 改造
背景： data_saver_service.py（后续将重新设计）将从websoket服务器采集数据，并进行数据保存，保存的基本思路是通过订阅的tag标签，存储到数据库表格对应的记录里。
因此，需要对vehicle_data表格进行改造，并且重新命名，要求、
1、在数据库初始化时候，创建预先数据行，
   数据来源于savedatabase\deviceConfig.json，需要记录的字段是plc，tag，RBindex， remark
2、后续存储字段按vehicle_data表格字段定义，排除id